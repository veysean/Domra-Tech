import React from "react";

const RequestCard = ({ imageSrc, desc, buttonText }) => {
    return (
        <div className="flex items-center p-4 border-b border-gray-200">
           
        </div>
    );
};

export default RequestCard;
